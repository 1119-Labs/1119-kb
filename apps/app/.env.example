# =============================================================================
# Knowledge Agent Template - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# See docs/ENVIRONMENT.md for detailed descriptions.

# Disable Nuxt telemetry prompt so dev server starts without waiting for input
# NUXT_TELEMETRY_DISABLED=1

# --- Database (recommended for local dev with Bun) ---------------------------
# When unset, Nuxt Hub uses PGLite (embedded). PGLite can hit "Aborted()" under Bun.
# Set DATABASE_URL to a real PostgreSQL to avoid that (e.g. local Postgres or Neon).
# DATABASE_URL=postgresql://user:password@localhost:5432/your_db

# --- Authentication ----------------------------------------------------------

BETTER_AUTH_SECRET=          # Random secret for signing sessions (generate with: openssl rand -hex 32)
# BETTER_AUTH_URL=https://kb.example.com
# BETTER_AUTH_TRUSTED_ORIGINS=https://kb.example.com,https://www.kb.example.com
GITHUB_CLIENT_ID=            # GitHub OAuth app client ID
GITHUB_CLIENT_SECRET=        # GitHub OAuth app client secret

# --- AI ----------------------------------------------------------------------

OPENROUTER_API_KEY=          # OpenRouter API key (required for chat, title generation, bots, OCR). Get from https://openrouter.ai/keys

# --- Sandbox & Sync ----------------------------------------------------------

# NUXT_GITHUB_SNAPSHOT_REPO= # Default snapshot repository (owner/repo format)
# NUXT_GITHUB_SNAPSHOT_BRANCH=main
# NUXT_GITHUB_TOKEN=         # Optional PAT override for snapshot git operations
#                           # If omitted, the app uses GitHub App installation tokens

# --- GitHub Bot (optional) ---------------------------------------------------

# NUXT_PUBLIC_GITHUB_APP_NAME=your-bot-name     # GitHub App name (used for install URL and bot username)
# NUXT_PUBLIC_GITHUB_BOT_TRIGGER=               # Override bot mention trigger (defaults to app name)
# NUXT_GITHUB_APP_ID=
# NUXT_GITHUB_APP_PRIVATE_KEY=
# NUXT_GITHUB_WEBHOOK_SECRET=

# --- Discord Bot (optional) --------------------------------------------------

# NUXT_PUBLIC_DISCORD_BOT_URL=               # Full Discord bot invite URL
# NUXT_DISCORD_BOT_TOKEN=
# NUXT_DISCORD_PUBLIC_KEY=
# NUXT_DISCORD_APPLICATION_ID=
# NUXT_DISCORD_MENTION_ROLE_IDS=

# --- YouTube (optional) ------------------------------------------------------

# NUXT_YOUTUBE_API_KEY=

# --- Storage (optional) ------------------------------------------------------

# BLOB_READ_WRITE_TOKEN=     # Vercel Blob token for file uploads

# --- State (optional) --------------------------------------------------------

# REDIS_URL=                  # Redis URL for bot state persistence (falls back to in-memory)

# --- Docker non-interactive Vercel auth (optional) ---------------------------
# Use this if you want Docker to run `vercel env pull` at startup without browser login.
# You still need a linked project in apps/app/.vercel/project.json.
#
# AUTO_PULL_VERCEL_OIDC_TOKEN=1
# VERCEL_TOKEN=               # Vercel personal/team access token
# VERCEL_TEAM_ID=             # Team ID (optional but recommended in multi-team accounts)
